# Tests

Automated checks for the repository's own content and tooling — structural
validation fixtures and any unit tests for `scripts/`. End-to-end validation
of the published content is run by
[scripts/bash/validate.sh](../scripts/bash/validate.sh); tests here cover the
tooling that script depends on.
