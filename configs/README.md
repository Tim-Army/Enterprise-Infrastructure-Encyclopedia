# Configs

Vendor and platform configuration examples referenced from chapters. Organize
by vendor/product subdirectory (for example `configs/cisco-ios-xe/`,
`configs/fortios/`) and link to files here from the chapter that uses them
rather than duplicating configuration blocks inline.
